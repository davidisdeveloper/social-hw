import styles from '../App.module.css';

function One() {

    return(
        <div className={styles.mainView}>
            <p className={styles.title}>열대 저기압<br />(a.k.a 태풍)</p>
        </div>
    )
}

export default One;